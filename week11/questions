1. Передача передает основные свойства аргументов : перемещаемый остается перемещаемым, модифицируемый модифицируемым, а констаный как объект для чтения. 
Реализация :
template <typename T1, typename T2>
void wrapper(T1&& e1, T2&& e2) {
    func(forward<T1>(e1), forward<T2>(e2));
}
2. Вышеупомянутая T&& для параметра T, параметр может быть любым (конст и не конст, указывать на объект). В  Тип&& - для типа параметр объявляется как rvalue (только перемещаемый)
3. Ошибка подстановки не ошибка: ознаечает что при определении перегрузки функции ошибки шаблонов не вызывают ошибки компиляции. Компилятор должен рассмотреть наилучшее соответсвие передаваемым аргументам.
Но ошибки будут если не найдется нужной перегрузки, нашлось несколлько.
4. Упаковка: возвращаемого типа функции, нешаблонного аргумента. Добавление: параметра функции или шаблона с аргументом по дефолту. 
5.???
